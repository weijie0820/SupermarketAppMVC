<!DOCTYPE html>
<html>
<head>
  <title>Admin Dashboard - User Management</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

       <%- include('partials/navbar') %>


    <div class="container mt-5">

            <h2>User Management</h2>
            <p>Change user roles: Member ↔ Admin</p>

            <% if (users.length === 0) { %>
                <p>No users found.</p>
            <% } else { %>

            <table class="table table-bordered text-center">
                <thead>
                <tr>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Current Role</th>
                    <th>Update Role</th>
                </tr>
                </thead>
                <tbody>
                <% users.forEach(function(user) { %>
                    <tr>
                    <td><%= user.username %></td>
                    <td><%= user.email %></td>
                    <td><%= user.role %></td>

                <td>
                    <!-- UPDATE ROLE FORM -->
                    <form action="/admin/users/update-role" method="POST" class="d-flex gap-2 mb-2">
                        <input type="hidden" name="user_id" value="<%= user.id %>">

                        <select name="new_role" class="form-select">
                            <option value="user" <%= user.role === 'user' ? 'selected' : '' %>>Member</option>
                            <option value="admin" <%= user.role === 'admin' ? 'selected' : '' %>>Admin</option>
                        </select>

                        <button class="btn btn-primary btn-sm">Save</button>
                    </form>

                    <!-- DELETE USER FORM -->
                    <form action="/admin/users/delete" method="POST"
                        onsubmit="return confirm('Are you sure you want to delete this user?');">
                        <input type="hidden" name="user_id" value="<%= user.id %>">
                        <button class="btn btn-danger btn-sm w-100">Delete</button>
                    </form>
                </td>
                </tr>
            <% }) %>
                    </tbody>
                </table>

        <% } %>

        <a href="/inventory" class="btn btn-secondary mt-3">Back</a>
    </div>

</body>
</html>
